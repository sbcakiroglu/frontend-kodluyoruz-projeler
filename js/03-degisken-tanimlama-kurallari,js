//camelCase kullanımı:
let fullname = "" //hatalı kullanım
let kodluyoruzserverinfo= "" // hatalı kulalnım
let kodluyoruz_server_info= "" //hatalı kullanım
let fullName = ""
let kodluyoruzServerInfo = "" 
let kodluyoruzSERverInfo// hatalı kulalnım. js büyük küçük harf DUYARLI

// UPPER_CASE kullanım:
const password = "1234" //hatalı kullanım
const PASSWORD = "1234" // dogru kullanım
const SERVER_PASSWORD = "1234"

//değiskenelrde turkce ve dieğr diller kullanımı:
let türkçeBilgi = 'Türkçe Bilgi Yazdırılıyor' //hatalı kullanım
console.log(türkçeBilgi)

let info = 'Türkçe Bilgi Yazdırılıyor'
console.log(info)

//anlamsız değisken adları kullanmayın!:
let x ="1"; // bu kullanım dogru değil
// x, y, z, k, i, e, gibi anlamsız değişken isimleri kullanmayın

//EK BİLGİ
// boolean tanımlarken is/has kullanımı:
let isActive = true
let hasPassword = false

//line length < 80
 